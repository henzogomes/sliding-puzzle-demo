import React from 'react';
import { HoverCardProps } from './types';

const HoverCard = (props: HoverCardProps) => {
  const { triggerText, content } = props;

  // @TODO: Implement Radix Hover Card.
  return (
    <div />
  );
};

export default HoverCard;
